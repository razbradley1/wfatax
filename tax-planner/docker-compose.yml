version: "3.9"

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - db-data:/app/data
    environment:
      - DATABASE_URL=sqlite:///data/tax_planner.db

volumes:
  db-data:
